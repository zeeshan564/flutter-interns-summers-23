import 'package:flutter/material.dart';

class S1Settings extends StatelessWidget {
  const S1Settings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Settings Screen'),
    );
  }
}
