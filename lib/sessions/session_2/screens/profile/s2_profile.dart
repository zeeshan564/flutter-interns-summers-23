import 'package:flutter/material.dart';

class S2ProfileScreen extends StatelessWidget {
  const S2ProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile'),
      ),
    );
  }
}
