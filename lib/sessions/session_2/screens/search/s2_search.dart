import 'package:flutter/material.dart';

class S2SearchScreen extends StatelessWidget {
  const S2SearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Search'),
      ),
    );
  }
}
