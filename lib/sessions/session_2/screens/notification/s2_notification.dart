import 'package:flutter/material.dart';

class S2NotificationScreen extends StatelessWidget {
  const S2NotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Notification'),
      ),
    );
  }
}
