import 'package:flutter/material.dart';

class S2AddPost extends StatelessWidget {
  const S2AddPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Post'),
      ),
    );
  }
}
