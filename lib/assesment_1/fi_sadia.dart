import 'package:flutter/material.dart';

class FIASadia extends StatelessWidget {
  const FIASadia({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Sadia"),
      ),
      body: const Center(
        child: Text("Hello! I am Sadia"),
      ),
    );
  }
}
