import 'package:flutter/material.dart';

class FISaleh extends StatelessWidget {
  const FISaleh({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Made by Saleh"),
      ),
      body: const Center(
        child: Text("Hello to all flutter interns!"),
      ),
    );
  }
}
