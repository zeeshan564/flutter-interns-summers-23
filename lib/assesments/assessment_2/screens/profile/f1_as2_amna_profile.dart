import 'package:flutter/material.dart';

class F1As2AmnaProfile extends StatelessWidget {
  const F1As2AmnaProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Profile")),
    );
  }
}
