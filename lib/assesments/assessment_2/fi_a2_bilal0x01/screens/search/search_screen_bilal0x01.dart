import 'package:flutter/material.dart';

class SearchScreenBilal0x01 extends StatelessWidget {
  const SearchScreenBilal0x01({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Search here",
      ),
    );
  }
}
