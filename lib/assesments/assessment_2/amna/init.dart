import 'package:flutter/material.dart';

class Assessment2Init extends StatelessWidget {
  const Assessment2Init({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}