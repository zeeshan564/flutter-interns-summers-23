part of '../fi_as2_amna_profile_card.dart';

class _FiAs2AmnaTaggedView extends StatelessWidget {
  const _FiAs2AmnaTaggedView();

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(50),
      child: Center(
        child: Text("Video"),
      ),
    );
  }
}
